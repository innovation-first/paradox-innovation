apiVersion: apps/v1
kind: Deployment
metadata:
  name: blog
  labels:
    app: nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: registry.thalesdigital.io/tsn/innovation/projects/blog/blog:latest
          ports:
            - containerPort: 80
      imagePullSecrets:
        - name: regcred
